cmake_minimum_required(VERSION 3.16)
project(TestSubmoduleProject LANGUAGES C)

# Add the Protocol Toolkit as a subdirectory (simulating submodule)
add_subdirectory(src protocol_toolkit)

# Create a simple test executable that uses the library
add_executable(test_app test_main.c)
target_link_libraries(test_app ProtocolToolkit::macos)

message(STATUS "Test project configured with Protocol Toolkit as submodule")
