#include "ptk_pdu.h"
#include <assert.h>

void test_ptk_pdu(void) {
    // Only macro system present, so just check header inclusion and macro expansion
    assert(1);
}

int main(void) {
    test_ptk_pdu();
    return 0;
}
