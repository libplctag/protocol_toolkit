# Examples CMakeLists.txt
# Build all example implementations

# Add subdirectories for each example
# add_subdirectory(ethernetip)
# add_subdirectory(modbus)

message(STATUS "Building howto example")
add_subdirectory(howto)